<?xml version="1.0" encoding="UTF-8"?>
<!--
  This file was previously used for IntelliJ IDEA-specific VCS configuration (VcsDirectoryMappings).
  IDE-specific configuration should not be committed to the repository and should instead be ignored
  via .gitignore (for example, by ignoring the entire .idea directory).

  The contents have been intentionally cleared to avoid storing IDE metadata in version control.
  Consider removing this file from the repository and adding .idea/ to .gitignore.
-->